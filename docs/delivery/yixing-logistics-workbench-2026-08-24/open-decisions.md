# Open Decisions and Administrator Inputs

These are the exact remaining inputs. Do not place credential values in chat, Git, screenshots or evidence; rotate them at the provider, then create Harness Secret entries by identifier only.

| ID | Required decision/evidence | What the responsible administrator must do | Blocking gate / owner |
| --- | --- | --- | --- |
| GIT-001 | Review policy (partially resolved) | `main` now exists at audited commit `21d209fe970fef89dfe3ca8052efe4437796f48b` and is the default branch; configure required checks, CODEOWNERS/review count and merge policy after CI exists. The original evidence branch is retained | Product / Repository admin |
| HAR-001 | Target Harness binding (partially resolved) | Account `VR3gJDjcRkq7ocR7ywpT-Q`, Org `default`, Project `yixing_logistics_workbench` and Environment IDs `yixing_dev`, `yixing_preprod`, `yixing_prod` are bound. Approve Service and Pipeline identifiers after HAR-002 is decided | Product / Harness project admin |
| HAR-002 | Runtime and connectors (partially resolved) | The initiator confirmed access is limited to the local computer, company intranet or VPN and selected the current Windows Server 2025 host for local development/testing. WSL 1 + Ubuntu 24.04, PostgreSQL 16 and Redis 7 are installed and locally reachable; this host may back logically isolated `yixing_dev`/`yixing_preprod` after the Service/deployment contract exists. Production remains unresolved because the single host has no independent failure domain, HA or recovery evidence. Source, registry, deployment and observability connector refs are still required | Design / Platform owner |
| HAR-003 | Approval and policy | Name Product, Design, Security, Acceptance and Production approver groups; specify freeze window, exemption policy and CV/SLO refs | Product + Release / Project admin |
| G7-001 | Credential rotation (deferred for dependent systems) | Old G7 credentials are confirmed still active and used by other systems. Leave them unchanged and prohibit this project from using them. Coordinate owners and a cutover window before rotation; then save only the rotated values under `G7_TENANT_CODE`, `G7_SANDBOX_ACCESS_KEY`, `G7_SANDBOX_SECRET_KEY`, `G7_PROD_ACCESS_KEY`, `G7_PROD_SECRET_KEY` in the approved Harness scope and record only rotation timestamp/key fingerprint | Product/Design / G7 tenant admin + Secret admin |
| G7-002 | SANDBOX application contract (credentialless work in progress) | Workbench access, tenant-admin role and dedicated target application `ZHLDEMO` are confirmed. It is enabled with 41 active scopes; SANDBOX declares 82 supported/25 unsupported REST endpoints and 18 message scopes. Build fail-closed G7 ports and synthetic offline contract fixtures now; do not claim behavioral evidence. After coordinated rotation, complete Harness Secret binding and controlled signed SANDBOX tests for authorization, errors, pagination/cursor, callbacks, quotas and retention | Product/Design / G7 integration owner |
| OQ-006 | G7 limits/signature/retention | Provide target-tenant limits, signing rotation/clock-skew rules, event retention, media authorization lifetime and callback allowlists | Product/Design / G7 + Security |
| OQ-007 | Location retention | Approve whether raw location may be stored, its precision, region, retention, access/export/delete and legal hold behavior | Design / Data protection + Product |
| OQ-008 | Data-scope semantics (proposal recorded) | ADR-0003 proposes intersection across tenant, role, organization, tag, fleet, assignment, shift and delegation with explicit deny precedence. Security and Product must approve or revise this rule before runtime policy or RLS implementation | Design / Security + Product |
| OQ-009 | Model/data governance | Select provider/model/region; approve retention, training use, redaction, prompt/tool logs, budgets, evaluation thresholds and kill switch | Design / AI governance + Security |
| AUTH-001 | Workforce and driver identity (direction confirmed) | The initiator confirmed project-owned identity for local/intranet/VPN use and continued with recommended option A. ADR-0003 proposes self-hosted Keycloak, OIDC Authorization Code + PKCE, strong workforce MFA, application-owned TenantScope/RBAC/ABAC, task-bound driver sessions and separated R3 approvers. Security, Product and Data owners must approve OQ-008, `G7-GAP-001/008`, production identity infrastructure and named approver groups before implementation | Design / Security architect + Product + Data owner |
| DATA-001 | PostgreSQL/Redis and retention | Select managed services, region, HA, encryption/KMS, backup/restore objectives, tenant isolation, audit/order/media retention and private connectivity | Design / Platform + Data owner |
| MAP-001 | AMap production integration | Rotate the key found in the prototype; create `AMAP_WEB_KEY` and `AMAP_SECURITY_JS_CODE` secrets; configure domain whitelist/proxy/quota and approve degraded behavior | Design / Platform + Security |
| MODEL-001 | Agent provider | After OQ-009, create `MODEL_PROVIDER_API_KEY` only if V1-C is enabled; repair or replace the currently failing model connector | Design / AI governance |
| RELEASE-001 | Rollout and rollback | Approve canary or rolling percentages, observation windows, SLO thresholds, freeze windows, rollback authority and last-known-good ownership | Release / Release manager + SRE |
| STORY-001 | Missing acceptance IDs | Add stable TEST IDs and Given/When/Then clauses for US-069..074; correct the obsolete “68 FR” Build Ready assertion without renumbering existing TEST-001..145 | Product / Product owner + QA |

The current GitHub URL is sufficient and the source assets are no longer missing. Remaining items above are genuinely absent or undecided; broad authorization does not replace the action-specific approval required for provider-side credential invalidation or Secret mutation.
