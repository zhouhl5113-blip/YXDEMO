{
  "name": "@yixing/workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24.12 <25"
  },
  "scripts": {
    "format": "biome format --write --files-ignore-unknown=true package.json pnpm-workspace.yaml tsconfig.json tsconfig.base.json biome.json packages tests",
    "format:check": "biome format --files-ignore-unknown=true package.json pnpm-workspace.yaml tsconfig.json tsconfig.base.json biome.json packages tests",
    "lint": "biome lint --files-ignore-unknown=true package.json pnpm-workspace.yaml tsconfig.json tsconfig.base.json biome.json packages tests",
    "typecheck": "tsc --project tsconfig.json",
    "test": "node --test",
    "test:unit": "node --test \"tests/unit/**/*.test.ts\"",
    "test:contract": "node --test \"tests/contract/**/*.test.ts\"",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
